{
  "name": "list-tokens",
  "version": "0.1.0",
  "description": "Manage Zhipu GLM Coding Plan API keys and show their remaining quota.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build src/cli.ts --compile --outfile dist/list-tokens",
    "lint": "biome ci .",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:compiled": "bun run build && LIST_TOKENS_BIN=dist/list-tokens bun test",
    "check": "bun run lint && bun run typecheck && bun test && bun run test:compiled",
    "format": "biome check --write .",
    "prepare": "lefthook install"
  },
  "dependencies": {
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.14",
    "@types/bun": "^1.4.2",
    "lefthook": "^2.1.14",
    "typescript": "^7.0.2"
  }
}
